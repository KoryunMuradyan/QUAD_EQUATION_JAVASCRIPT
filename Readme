run the executable with command command like this "node exe filename".
there should be exist golden.txt file containing the right result (the last should be defined in golden.txt by user manually) of the equataion and also input.txt
then after execution if no error accurs the programm will compare the result generated by itself with the one from golden.txt 
afterwards an output.txt file containing the results (not matter wrong or right) will be generated
